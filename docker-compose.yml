services:
  postgres:
    image: postgres:16
    container_name: goprac-z5-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: study_security
    ports:
      - "5432:5432"